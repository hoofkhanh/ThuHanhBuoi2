#include<stdio.h>

int fabonaci(int n){
	if(n==0)
		return 0;
	if(n==1 || n==2)
		return 1;
	if(n>2);
	return fabonaci(n-1)+fabonaci(n-2);
}

int main(){
	int n;
	do{
		printf("nhap n");
		scanf("%d",&n);
	}while(n<0);
	printf("ket qua =%d ",fabonaci(n));

}
